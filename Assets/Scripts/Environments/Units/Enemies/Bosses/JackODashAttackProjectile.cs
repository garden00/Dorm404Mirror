using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class JackODashAttackProjectile : MonoBehaviour
{
    [SerializeField]
    private bool isDefensible = true;
    public bool IsDefensible => isDefensible;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
